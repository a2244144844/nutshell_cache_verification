# UC Test Report

This directory is reserved for template-style generated test reports.

Current generated coverage HTML:

```text
build/reports/cache_coverage.html
```

Primary report source:

```text
docs/coverage_report.md
unity_test/Cache_line_coverage_analysis.md
```

If a future Toffee run exports HTML directly into `uc_test_report/`, keep this README as the index and link the generated report files here.
